// DEPRECATED scaffold — retrieval now lives in Opus's operating-system-prompt.js
// (the COMPS set + FAMILY_BY_PATTERN). Kept only as a re-export so nothing
// breaks if referenced; safe to delete once confirmed unused.
export { FAMILY_BY_PATTERN } from '@/lib/operating-system-prompt'
